# 🎨 LessonCatalogUi

Интерфейс системы управления обучением, построенный на **Angular 21** и **Tailwind CSS**.

---

## 📋 Предварительные требования (Prerequisites)

Для запуска проекта вам понадобится среда выполнения **Node.js**.

### Установка Node.js:
1. **Официальный сайт:** Перейдите на [nodejs.org](https://nodejs.org/) и скачайте версию **LTS** (рекомендуется).
2. **Менеджер версий (рекомендуется):** Используйте [nvm](https://github.com/coreybutler/nvm-windows) для Windows или `nvm` для macOS/Linux. Это позволит легко переключаться между версиями.
   * Команда для установки нужной версии: `nvm install 22`
3. **Проверка установки:** Откройте терминал и введите:
   ```bash
   node -v
   npm -v

Быстрый старт
1. Установка зависимостей: В корне проекта выполните команду для загрузки всех библиотек (node_modules):
   ```bash
   npm install
2. Запуск сервера разработки:
   ```bash
   ng serve

🛠 Основные команды

## 📦 Основные команды

| Команда | Описание |
|--------|----------|
| `ng serve` | Запуск проекта с hot-reload |
| `ng build` | Сборка проекта в папку `dist/` для продакшена |
| `ng generate component name` | Создание нового компонента (без суффикса `Component`) |
| `ng test` | Запуск модульных тестов через Vitest |
| `ng lint` | Проверка кода на соответствие стандартам |

🏗 Архитектура проекта

 1) Signals — используются для реактивного управления состоянием (Angular 17+)
 2) Tailwind CSS — быстрая и современная верстка без лишнего CSS
 3) Reactive Forms — валидация и управление формами (в т.ч. в модальных окнах)
 4)  Stand-alone Components — проект не использует NgModule, что упрощает архитектуру и снижает связанность

📂 Структура папок
1) src/app/components — переиспользуемые элементы (модалки, кнопки).
2) src/app/pages — основные экраны приложения.
3) src/app/services — логика взаимодействия с API.
4) src/app/interfaces — описание типов и DTO.

